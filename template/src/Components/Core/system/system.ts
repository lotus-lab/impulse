import { system, ConfigStyle } from 'styled-system';

export const transform = system({
  prop: 'transform' as ConfigStyle,
  cssProperty: 'transform' as ConfigStyle,
});

export const transition = system({
  prop: 'transition' as ConfigStyle,
  cssProperty: 'transition' as ConfigStyle,
});

export const transformOrigin = system({
  prop: 'transformOrigin' as ConfigStyle,
  cssProperty: 'transformOrigin' as ConfigStyle,
});

export const cursor = system({
  prop: 'cursor' as ConfigStyle,
  cssProperty: 'cursor' as ConfigStyle,
});

export const tabIndex = system({
  prop: 'tabIndex' as ConfigStyle,
  cssProperty: 'tabIndex' as ConfigStyle,
});
